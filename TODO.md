[X] Max with on each page, max height | Consider mobile size as well
[X] Mobile frindly
[ ] each action should be a separated component for better consistency
[ ] Skeleton loading -> It shouldn't cause to change the components width/height
[ ] Errors should be handled getting from the BE side to be translated
[ ] Resizable columns
[X] Show professional tooltip on actions and New button
[ ] Extract all the styles to the be correct files -> We can have common ones into a common style
[ ] Ask AI to optimize the code for app, remove unused code and make it more readable
[ ] Remove seed scripts
[ ] 