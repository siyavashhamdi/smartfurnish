/** Re-export shared table toolbar (single implementation under `shared/table`). */
import TableToolbar from "../shared/table/TableToolbar";

export default TableToolbar;
export type { TableToolbarProps } from "../shared/table/TableToolbar";
