/** Re-enable when SMS login verification is ready for production. */
export const LOGIN_OTP_ENABLED = false;

/** Re-enable when SMS signup verification is ready for production. */
export const SIGNUP_OTP_ENABLED = false;
