# Default keystore passwords for local release builds.
# Override with ANDROID_KEYSTORE_PASSWORD / ANDROID_KEY_PASSWORD in production CI.
ANDROID_KEYSTORE_PASSWORD=smartfurnish
ANDROID_KEY_PASSWORD=smartfurnish
